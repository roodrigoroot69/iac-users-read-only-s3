name: access_buckets
runtime:
  name: python
  options:
    virtualenv: venv
description: IaC for Account with limit access for list all Buckets in S3
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
